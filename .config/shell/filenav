# These files automatically update when edited/saved in vim:
# keys	filename				description

# These do not update automatically, but on the next new instance of a program:
cfv	${XDG_CONFIG_HOME:-$HOME/.config}/nvim/         # neovim config
cfz	$ZDOTDIR/.zshrc					# zsh (shell) config
cfwm    ${XDG_CONFIG_HOME:-$HOME/.config}/i3/config     # window manager config file
cfs	${XDG_CONFIG_HOME:-$HOME/.config}/shell/        # shell config (aliases, dirnav, filenav)
cfn	${XDG_CONFIG_HOME:-$HOME/.config}/newsboat/     # newsboat (RSS reader)


