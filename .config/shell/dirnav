# You can add comments to these files with #
gP   $HOME/proj
gH   $HOME/archive
gR   $HOME/res

gC   ${XDG_CONFIG_HOME:-$HOME/.config}
gS   ${XDG_SCRIPTS_DIR:-$HOME/.local/bin}
gsh  ${XDG_DATA_HOME:-$HOME/.local/share}
gcac ${XDG_CACHE_HOME:-$HOME/.cache}

gmn  /mnt
gO   /opt
